# Optional: Google AI Studio / Gemini API key (loads in the browser via Vite — demo/local only).
# https://aistudio.google.com/apikey
VITE_GEMINI_API_KEY=

# Optional: override model (default: gemini-2.0-flash). If you hit errors, try gemini-1.5-flash.
# VITE_GEMINI_MODEL=gemini-1.5-flash
